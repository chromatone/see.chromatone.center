export declare type RectSide = 'top' | 'right' | 'bottom' | 'left';
export declare type Vec2 = {
    x: number;
    y: number;
};
export declare type Box = {
    x: number;
    y: number;
    w: number;
    h: number;
};
/** Calculate the distance of two points. */
export declare function distanceOf(p1: Vec2, p2: Vec2): number;
/**
 * @param box
 * @param size Add `size` to all edges.
 */
export declare function growBox(box: Box, size: number): Box;
export declare function isPointInBox(point: Vec2, box: Box): boolean;
/** Calculate the control point. */
export declare function controlPointOf(target: Vec2, another: Vec2, sideOfTarget: RectSide): {
    x: number;
    y: number;
};
/** Return the entering angle of a rectangle side. */
export declare function angleOf(enteringSide: RectSide): number;
